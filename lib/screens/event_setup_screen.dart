import 'package:flutter/material.dart';

import '../widgets/app_placeholder_scaffold.dart';

class EventSetupScreen extends StatelessWidget {
  const EventSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppPlaceholderScaffold(
      title: 'Event Setup',
      message: 'Phase 1 placeholder for event creation layout.',
    );
  }
}