import 'package:flutter/material.dart';

import '../widgets/app_placeholder_scaffold.dart';

class LogsSearchScreen extends StatelessWidget {
  const LogsSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppPlaceholderScaffold(
      title: 'Logs & Search',
      message: 'Phase 1 placeholder for saved logs and search layout.',
    );
  }
}